info:
  name: delete-por-id
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseURL}}/alunos/11111"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
