info:
  name: recupera-alunos
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/alunos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
