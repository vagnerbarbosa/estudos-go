info:
  name: busca-por-id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/alunos/11111"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
