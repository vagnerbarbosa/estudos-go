info:
  name: atualiza-aluno
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseURL}}/alunos/1"
  body:
    type: json
    data: |-
      {
        "nome": "João Marsh",
        "cpf": "123434441234",
        "rg": "6534643563456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
