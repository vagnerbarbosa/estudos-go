info:
  name: Saudação personalizada
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/:nome"
  params:
    - name: nome
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Retorna uma saudação
    request:
      url: "{{baseUrl}}/:nome"
      method: GET
      params:
        - name: nome
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
