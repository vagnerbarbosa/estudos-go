info:
  name: Edita dados de um aluno
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/alunos/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Aluno atualizado
    request:
      url: "{{baseUrl}}/alunos/:id"
      method: PATCH
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
