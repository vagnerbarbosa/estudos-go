info:
  name: Deleta um aluno
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/alunos/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Aluno deletado
    request:
      url: "{{baseUrl}}/alunos/:id"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
