info:
  name: Busca aluno por ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/alunos/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Aluno encontrado
    request:
      url: "{{baseUrl}}/alunos/:id"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
