info:
  name: Busca aluno por CPF
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/alunos/cpf/:cpf"
  params:
    - name: cpf
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Aluno encontrado
    request:
      url: "{{baseUrl}}/alunos/cpf/:cpf"
      method: GET
      params:
        - name: cpf
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
