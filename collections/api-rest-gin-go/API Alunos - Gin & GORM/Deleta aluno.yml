info:
  name: Deleta aluno
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/alunos/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
