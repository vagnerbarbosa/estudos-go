info:
  name: Saudação
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/:nome"
  params:
    - name: nome
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
