info:
  name: Cria novo aluno
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/alunos"
  body:
    type: json
    data: |-
      {
        "nome": "Vagner Barbosa",
        "cpf": "12345678901",
        "rg": "1234567"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    description: Criado
    request:
      url: "{{baseUrl}}/alunos"
      method: POST
      body:
        type: json
        data: |-
          {
            "nome": "Vagner Barbosa",
            "cpf": "12345678901",
            "rg": "1234567"
          }
    response:
      status: 201
      statusText: Created
      body:
        type: text
        data: ""
