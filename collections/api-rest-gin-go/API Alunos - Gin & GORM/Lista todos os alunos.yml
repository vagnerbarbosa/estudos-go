info:
  name: Lista todos os alunos
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/alunos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/alunos"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
