info:
  name: Edita aluno
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/alunos/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "nome": "Vagner Editado"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
