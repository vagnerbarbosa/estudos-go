info:
  name: Busca por CPF
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/alunos/cpf/:cpf"
  params:
    - name: cpf
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
